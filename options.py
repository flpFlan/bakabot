# core:
MAX_CONNECT_RETRIES = 3  # if disconnected, this determined max times to reconnect

# pid:
PID_PATH = r"C:\wwwroot\www.flpflan.top\pixiv"  # physical address, where saves the file
PID_URL = r"http://flpflan.top/pixiv/"  # url to expose, should correspond to the physical folder
